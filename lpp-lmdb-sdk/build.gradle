plugins {
    id "com.jfrog.bintray" version "1.3.1"
}

apply from: "https://raw.githubusercontent.com/ametiste-oss/ametiste-bintray-gradle/master/bintray.gradle"

apply plugin: 'maven-publish'

description = 'LMDB L-Platform Protocol SDK'

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
            artifact sourcesJar {
                classifier "sources"
            }
            artifact javadocJar {
                classifier "javadoc"
            }
        }
    }
}

dependencies {
    compile("org.ametiste.lambda-platform:lambda-platform-sdk:${ameLaPlatformVersion}")
    compile("org.ametiste.lib:lmdb-connector-api:${ameLmdbConnectorVersion}")
}